keybind=ctrl+j=goto_split:previous
background-opacity = 0.96
# window-theme = ghostty
theme = Dayfox
font-family = "UbuntuMono Nerd Font"
font-size = 16
gtk-titlebar = false
#quick-terminal-size=100%,100%
shell-integration = fish

# Vim-mode keybindings live in vim-mode.conf (same directory).
config-file = vim-mode.conf

